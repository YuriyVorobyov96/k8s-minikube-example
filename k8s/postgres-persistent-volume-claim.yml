apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: psql-data-persisent-volume-claim
spec:
  volumeName: psql-data-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
