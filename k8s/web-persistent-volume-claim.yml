apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webapp-source-code-persisent-volume-claim
spec:
  volumeName: webapp-source-code-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
